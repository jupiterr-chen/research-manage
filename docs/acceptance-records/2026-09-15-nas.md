# 验收记录 2026-09-15 NAS(P3)

- 执行者:最终验收方(Claude,本会话)
- 代码版本:`release/v1.0` @ 8f03d97(基线 develop @ f7441cc = tag v1.0.0-rc1 + T-02/T-04/T-05/T-06/T-07 修正)
- 环境:NAS 192.168.1.150;TA 镜像 `sha256:c85a52e6…`;管理台镜像 `agents-manage-agents-manage`(NAS 本地构建);`.local/nas.env` 保存 token(不入库)
- 问题登记:[P3-TASKS.md](../P3-TASKS.md)

## A. 本地基线复核(develop @ f7441cc)

| 项 | 结果 | 证据 |
|---|---|---|
| `pytest tests/unit` | PASS 359 | 本会话输出 |
| `ruff check` / `format --check` | PASS(零告警,73 文件) | 本会话输出 |
| `scripts/fetch_vendor.sh` + `verify_vendor.sh` | PASS(80 文件哈希与 NAS 镜像一致) | 本会话输出 |
| `pytest tests/shape` | PASS 13 | 本会话输出 |
| `pytest -m real`(tradingagents-local + llm-stub) | PASS 3(11 分 15 秒) | 后台任务 blgsp6ps3 输出 |

## B. ACCEPTANCE §2 NAS 流程

| 步 | 结果 | 备注 |
|---|---|---|
| 1 fetch/verify/shape/real | PASS | 见 A |
| 2 sync_to_nas | PASS | `/home/chen/docker/agents-manage/` 1.0M;未含 `.env`/data/vendor;未触碰 TradingAgents 目录 |
| 3 build + up + healthz | PASS(经 T-05 处置) | DEPLOY §1 实测 1000:1000 / 994 / c85a52e 与文档一致;首次 up 因 root 创建 `data/` 失败(T-05),`rmdir → mkdir` 后 healthy;`/healthz` 无鉴权 401、带 token 200 `docker_ok=true worker_alive=true`;`/` 无鉴权 302 `/login`;`/api/v1/runs` 无鉴权 401;端口只发布在 192.168.1.150(T-02) |
| 4 真实标的发起 | PASS(经 resume) | 用户确认:1810.HK / 2026-09-15 / 全量四项。首次发起卡在 queued(T-06:Worker 在容器内用宿主路径 mkdir);修复并重建后 21:28:13 启动执行容器 `am-r-20260915-212531-1810HK`,21:28:26 status.json 已有 tokens(真实 LLM 调用)。run1 在 Fundamentals 阶段遇 relay 503 → `failed`(exit 1,error 原文,容器已 remove,断点保留,3 份分析师报告已落盘)。resume → run2 `r-20260915-213806-1810HK`(`resumed_from` 正确)21:38:07 启动,50 秒内已到 4/12(跳过已完成 3 个分析师),21:42:33 `succeeded` exit 0,12/12,tokens 112842↑/44102↓ |
| 5 全程观察 + AM-07 扫描 | PASS(系统侧)/ 待用户 grep | 三产物齐全:reports/ 7 个 md(含 investment_plan.md)、memory 第 22 行 `[2026-09-15 \| 1810.HK \| Hold \| pending]`、`full_states_log_2026-09-15.json` 262KB;`report_ready=true`;artifacts 返回 7 条 SMB 路径且不含内容;两次 run 的执行容器均已 remove;container.log 落档。**T-07**:run1 的 container.log 含上游打印的 FRED `api_key=` 真实值 → 已修 scrub 并现场重新脱敏 |
| 6 取消/续跑 | 部分 PASS(NAS cancel 由用户决定跳过)| failed→resume 分支已在真实环境验证(见 4);cancel(SIGINT→130→cancelled)分支在本地真实代码 `-m real` 验证通过;NAS 上是否再做一次 cancel 由用户决定(需再消耗一次额度,且同标的同日期需换日期/标的以免覆盖今日报告) |
| 7 LAN 访问三页 | PASS(curl+Bearer) | `/`、`/instruments`、`/runs/<id>`、`/fragments/overview` 均 200;动态区 `hx-trigger="every 15s"`;页面无 `http(s)://` 外链;详情页显示 Market Analyst 进行中、container.log 路径。浏览器登录由用户自行验证(验收方不代填 token) |
| 8 正式调度 ≥3 交易日 | 已配置,观察中 | 2026-09-15 21:50 配置 1810.HK:`daily_trading 08:30 日频三件套` + `weekly 周一 08:30 全量`(附 A 决策 5 默认值);`/healthz scheduler_jobs=2`;页面 describe 文案与「周一将只跑全量」提示正确。观察窗口:09-16(三)、09-17(四)、09-18(五)日频;09-21(一)验证去重只跑全量。**每日核对项**:08:30 后 run 自动入队并 succeeded、report ✓、无重复 run、无 stale。
**09-16(三)观察**:调度触发 3 条(0700.HK 08:30 / 1810.HK 08:35 / 2015.HK 08:40,用户前一晚加的 HK 标的)全部 `succeeded`,trigger=schedule;另有用户手动 9988.HK 一条 succeeded。用户随后把 HK 布局改为「周一周频全量 06:00~07:30(30 分钟间隔)+ 日频 07:51~08:22(10 分钟间隔)」,由此暴露 **T-10**(已 succeeded 的同日调度会重跑)→ 当晚修复并部署(rc3)。21:46 通过 `scripts/apply_schedules.py` 为 4 只美股建 8 条调度(周一全量 19:00/19:30/20:00/20:30,日频 20:50/21:00/21:10/21:20),`scheduler_jobs=16`。 |
| 9 记录/合并/tag | 待 | |

## C. AM 项(NAS 侧)

| AM | 结果 | 证据 | 备注 |
|---|---|---|---|
| AM-03(NAS) | PASS | B.4/B.5 | exit 0 + investment_plan.md + memory 行 + full_states_log 三者齐全 |
| AM-04(NAS) | PASS(resume 分支)| B.4 | 断点续跑跳过已完成节点;cancel 分支以本地 `-m real` 结果为准 |
| AM-06 | PASS | B.3 | 生产为 0.0.0.0 + token(容器内),门禁按 SPEC 生效 |
| AM-07(NAS) | PASS | 用户 2026-09-15 在 NAS 执行真实 key 值 grep(data/ + compose logs):仅 `scan-done`,无 LEAK(T-07 修复与重脱敏之后);`docker inspect` 执行容器:`Config.Env` 仅 `AM_RUN_ID`、`TZ` + 镜像自带 PATH/LANG/PYTHON_*;挂载 4 项与 SPEC §6.3 逐项一致,`.env` 为 ro;管理台启动日志 token 显示 `***` | |
| AM-11(对等) | PASS | A | |
| AM-13/18(NAS) | PASS | audit 3 行(instrument create / create_run ×2,resume 的 detail 含 `resumed_from`,actor=api);`report_ready` 与文件实况一致;container.log 存在、页面只显示路径 |

## 缺陷清单

见 [P3-TASKS.md](../P3-TASKS.md):T-01(S3 open)、T-02(fixed)、T-03(wontfix)、T-04(fixed)、T-05(脚本/文档 fixed,代码部分 open,S2)、T-06(S1,fixed 8b94f16)、T-07(S1 密钥泄漏,fixed 8f03d97)、T-08(S3 open)。

## 结论

**阶段性结论(2026-09-15)**:🔴 项在 NAS 侧全部 PASS(AM-03/04/06/07/11/13/18;AM-04 cancel 分支以本地真实代码结果为准,用户决定不在 NAS 重复)。P3 期间发现并修复 3 个 S1(T-05 部署、T-06 路径、T-07 密钥泄漏)、1 个 S2(T-02),遗留 T-01/T-05 代码项/T-08 为 S3/S2 非阻塞。
生产已上线:`release/v1.0` @ f7b3ec9(tag v1.0.0-rc3 = rc2 + T-10),调度已启用(4 HK + 4 US)。**最终结论待第 8 步 3 个交易日观察后给出**,届时合并 main 并打 v1.0.0。

## D. v1.2 财报原文获取(reports-fetcher)上线记录(2026-09-22)

- 代码:`release/v1.0` @ 7711072(develop S10 合入 + T-11);NAS 重建 `agents-manage-agents-manage-1`。
- 生产配置:`deploy/.env` 增 `REPORTS_API_BASE_URL=http://serve:8000`;管理台加入外部网络 `reports-fetcher_default`(T-11:`host.docker.internal` 只到网桥网关,reports-fetcher 发布在宿主回环 127.0.0.1:8000,实测 Connection refused;不改 reports-fetcher)。
- 探针(用户指定 0700.HK):
  1. 只读:容器内 `GET http://serve:8000/health/ready` → `{"status":"ok"}`;`GET /api/v1/archive/reports?code=0700.HK` → 返回既有归档(HKEX 真实来源)。
  2. 小任务 `last_n=1, refresh=false`:`rj-20260922-140724-0700HK` → 5s 内提交 `job_22ece…`,15s 到终态 **partial**(1 份报告 `report_period=null / period_source=unknown`,warnings 21 条「仅有年份标签、无期末日证据,不猜测」;summary cached=1,未重复下载);`report_ids=[2727be0c…]`。
  3. 归档详情:`中期報告 2026 INTERIM`,artifact 5,451,089 B;代理下载 200,`X-Checksum-SHA256` = ETag = 本地 sha256 `6231374b…`,`%PDF-1.7`;`If-None-Match` → 304。
  4. `/healthz.reports` → `enabled/alive/reachable=true`;详情页显示「部分完成:可用文件已保留」「未知」「下载原文」。
- 结论:AM-19/25/28/33 生产侧 PASS;partial 语义、未知报告期不推测、sha256/ETag/304 全部按契约。无 mock 头进入生产(`app/` 无相关常量,AM-31 静态检查)。

### D.1 契约更新后复测(2026-09-22 19:40)

背景:上一轮反馈的 4 点(报告期 null、文件名、warnings 噪音、progress 初值)由 reports-fetcher 修复;
本系统同步只读副本 `integration-kit/`(套件基线 31→33 OK)并做两处改动:T-12(转发 `filename*` + 可读回退名)、T-13(展示 `coverage.notices`)。
生产:fetcher 镜像 `7f5fbad`(09-22 构建);管理台 `release/v1.0` @ dc437bf 重建。

| 项 | 修复前(D) | 复测(D.1) |
|---|---|---|
| `report_period` / `period_source` | `null` / `unknown` | **`2026-06-30` / `document`**(从 PDF 正文提取) |
| 任务终态 | `partial` | **`succeeded`** |
| warnings | 21 条逐条「不猜测」 | **2 条**,均来自 `coverage.notices`(报告组截断、19 个未选入候选) |
| `progress` 首次轮询 | `{0,0}` | **`{symbols_total:1, symbols_finished:0}`** |
| 上游文件名 | `unknown__INTERIM______ 2026__….pdf` | **`HK_00700_INTERIM_2026-06-30_….pdf`** + `filename*` |
| 代理下载 | 我方组装回退名 | 上游名可读 → **原样转发**,`X-Checksum-SHA256`=ETag=实际 sha256 `6231374b…`,`%PDF-1.7`,`If-None-Match`→304 |
| 详情页 | 「部分完成」 | 「已完成」「覆盖 1/1」「提示:共发现 20 个逻辑报告组…」(T-13 生效) |

`summary.cached=1`(未重复下载);`/healthz.reports` enabled/alive/reachable 全 true。
结论:AM-19/20/25/28 生产侧复测 PASS;上一轮 5 项反馈全部闭环。
