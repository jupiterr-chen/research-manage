# 验收记录 2026-09-15 LOCAL

- 执行者:开发 agent(ZCode,feat/acceptance 分支)
- 代码版本:feat/acceptance @ 0c66985(基于 develop 07f07d6)
- 环境:`tradingagents-sim:latest`(本地构建)/ `tradingagents-local:latest`(vendor 构建)/
  管理台镜像 `research-manage-agents-manage`(compose.local 构建)/ Docker Desktop 29.7.2
- 执行方式:`pytest -m acceptance`(33+1 项自动化)+ compose.local 手动全链路演练

| AM | 结果 | 证据 | 备注 |
|---|---|---|---|
| AM-01 | PASS | `tests/acceptance/test_acceptance_local.py::TestAM01Busy` | 409 busy 体 current/queued、容器数=1、run 行=1、无 B 审计;页面内联 |
| AM-02 | PASS | `TestAM02Fifo` | 3 调度按 id 入队、started_at 顺序、全部 succeeded、3 条 schedule 审计 |
| AM-03 a/b/c | PASS | `TestAM03DoubleVerdict` | no_report→failed(investment_plan.md);no_memory→failed(trading_memory);ok→succeeded 三产物齐全 |
| AM-03 d | PASS | `tests/acceptance/test_am03_real.py`(-m real,6:25) | tradingagents-local+llm-stub,3/4 分析师 succeeded,agents_total 11/12 |
| AM-04 | PASS | `TestAM04CancelResume` | SIGINT→130、断点保留、resume 复制快照、token=840<全量证明跳节点、带 analysts 的 resume→400 |
| AM-05 | PASS | `TestAM05Watchdog` | 缩短阈值→cancelled(watchdog_timeout)、容器清除。**偏差**:无 run.watchdog 审计(actor 枚举无系统角色,见缺陷清单) |
| AM-06 | PASS | `test_acceptance_static.py::TestAM06BindMatrix` | 8 组合拒绝(退出码 2)+回环无 token 启动 + 32 字符 token 启动且日志无 token 值 |
| AM-07 | PASS | `TestAM07Secrets` | 假密钥 5 项扫描(页面/API/审计/container.log/inspect Env)全空;app/ 无读取 .env 代码路径 |
| AM-08 | PASS | `TestAM08CorruptStatus` | 损坏窗口 stale=1、agents_done 保持、恢复、最终 succeeded |
| AM-09 | PASS | `TestAM09Recovery` | a 孤儿→host_restarted;b 容器在→接管至 succeeded;c 2 条 queued 按序执行;healthz alive |
| AM-10 | PASS | `TestAM10DedupForce` | 周一双调度→1 条全量 + deduped 审计;already_done→409;force→新 run |
| AM-11 | PASS | `TestAM11UpstreamSelfCheck` | 退出码 2、error=upstream_api_changed(run 与 status.json 一致);shape 对等见 `pytest tests/shape`(13 项)与 `scripts/verify_vendor.sh`(80 文件一致) |
| AM-12 | PASS | `TestAM12TradingDays` | 周六/周日 next_fires 为空且下一触发=周一;工作日触发入队 |
| AM-13 | PASS | `TestAM13AuditAndReportTruth` | 8 类写操作 actor 正确(web/api);删 investment_plan.md 后 artifacts 不再列出(实现取「或」分支的 artifacts 实况;report_ready 终态后不回写,系记录注明项) |
| AM-14 | PASS | `TestAM14NoNpmCdn` | 镜像内 which node/npm 空;模板/CSS 无 http(s)://;htmx 本地引用(离线浏览器操作属人工项,由无外链+本地资产覆盖) |
| AM-15 | PASS | `TestAM15PollingNoWs` | every 15s 属性存在;app/ 无 WebSocket/EventSource/event-stream |
| AM-16 | PASS | `TestAM16AtomicAndTotal` | 高频采样 status.json 全部有效;agents_total 11/12 |
| AM-17 | PASS | `TestAM17Whitelist` | API 5 类非法→400;`1810.hk`→normalize 接受;runner 非法 analysts 退出码 1 |
| AM-18 | PASS | `TestAM18ContainerLog` | container.log 含异常栈;页面/API 无日志内容(run.error 全文渲染系 SPEC §7.3 要求,与日志内容以 `[runner]` 行标记区分断言) |

## compose.local 全链路演练(2026-09-15 20:40)

- `docker compose -f deploy/docker-compose.local.yml up -d --build` → healthy
- `/healthz`:`{"status":"ok","worker_alive":true,"docker_ok":true,...}`
- `POST /api/v1/runs {code:AAPL}` → `r-20260915-204049-AAPL`:queued → running → **succeeded**,
  report_ready=true;`runs/<id>/` 含 container.log 与 status.json;artifacts 返回 6 个报告路径
- 启动日志配置摘要中 token 显示为 `***`(summary 不含值 + scrub 双重兜底)

## NAS 项(ACCEPTANCE §2)

- N/A(由最终验收方在 P3 执行;本记录仅 LOCAL)

## 缺陷清单

| # | AM | 描述 | 处理 |
|---|---|---|---|
| 1 | AM-05 | 手册预期 audit 有 `run.watchdog`,但 SPEC 约束 8 的 actor 枚举(web/api/schedule)无系统角色,worker 内部转移未留审计 | 未改动(需用户决定是否扩 actor 枚举——SPEC 变更) |
| 2 | AM-03b | 手册预期 error 含 `trading_memory`,原实现文案为「memory 条目」 | 已修:verdict 文案补全为「memory 条目(trading_memory.md)」(随本分支) |
| 3 | AM-04⑤ | 手册预期 resume 带不同 analysts 被拒;原实现忽略请求体 | 已修:resume 端点 extra=forbid,多余字段→400(随本分支) |

## 结论

可上线(LOCAL 范围):🔴 全部 PASS;2 处实现偏差已修复,1 处审计留痕偏差待用户拍板(不影响 🔴 判定)。
